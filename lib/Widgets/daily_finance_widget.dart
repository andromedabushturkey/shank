import 'package:flutter/material.dart';

class DailyFinanceWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(child: Center(child: Text('daily')));
  }
}
