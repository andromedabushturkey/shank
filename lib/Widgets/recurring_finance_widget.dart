import 'package:flutter/material.dart';

class RecurringFinanceWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(child: Center(child: Text('Recurring')));
  }
}
